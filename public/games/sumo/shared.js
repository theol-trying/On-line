// Constantes géométriques SUMO — partagées par le serveur (games/sumo/server.js) ET le client (client.js).
// AR0 = côté de l'arène carrée à 2 lutteurs · RING0 = rayon du dohyō (cercle de paille) · PR = rayon d'un lutteur.
// Arène et cercle grandissent avec le nombre de participants (facteur k côté serveur, renvoyé dans `ar`/`ring0`).
export const AR0 = 600, RING0 = 240, PR = 22;
